
public class ClasePrincipal {

		public static void main(String[] args) {
			//Se llama al metodo para mostrar todo por pantalla, se le manda la lista como variable, es el return de parsear()
			Pantalla.mostrar(JSON.parsear());
		}
}
